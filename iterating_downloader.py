from image_grabber import grab_image
from values import *

def iterating_download(download_ammount, first_id):
    # Grab first id image
    grab_image(first_id)

    for ammount_to_download in range(download_ammount):
        imageId = ""

        # Create letters for Id

        # Create number for Id

        grab_image(imageId)